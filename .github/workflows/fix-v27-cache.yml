name: Fix V27 loader cache
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "V27 automatic search rewriting disabled. Search layout is maintained directly in admin-v27-loader.js."