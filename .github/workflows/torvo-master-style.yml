name: TORVO Admin Master Style
on:
  push:
    paths:
      - '.github/workflows/torvo-master-style.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply Admin workspace fix V16
        run: python3 .github/torvo_master_style_patch.py
      - name: Commit Admin workspace fix V16
        run: |
          git config user.name "TORVO Admin Bot"
          git config user.email "actions@users.noreply.github.com"
          git add admin.html
          git diff --cached --quiet && exit 0
          git commit -m "Apply Admin workspace fix V16"
          git push
