name: TORVO Admin Master Style
on:
  push:
    paths:
      - '.github/workflows/torvo-master-style.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply safe Add Item master layout
        run: python3 .github/torvo_master_style_patch.py
      - name: Commit safe Add Item master layout
        run: |
          git config user.name "TORVO Admin Bot"
          git config user.email "actions@users.noreply.github.com"
          git add admin.html
          git diff --cached --quiet && exit 0
          git commit -m "Use single Add control in Add Item masters"
          git push
