import{supabase}from'./supabase';const ok=r=>{if(r.error)throw r.error;return r.data||[]};export const approvalService={async pending(limit=200){return ok(await supabase.rpc('get_pending_approval_queue',{p_limit:limit}))},async mine(limit=200){return ok(await supabase.rpc('get_my_approval_work',{p_limit:limit}))},async permissions(){return ok(await supabase.rpc('get_approval_permission_users'))},async decide(id,decision,note='',module='',entityType=''){const name=module==='payment'?'decide_payment_approval':module==='reversal'&&['sales_return','purchase_return'].includes(entityType)?'decide_return_approval':'decide_approval_request';const r=await supabase.rpc(name,{p_approval:id,p_decision:decision,p_note:note||null});if(r.error)throw r.error;return r.data},async setPermission(userId,canApprove,canApproveOwn=false){const r=await supabase.rpc('set_transaction_approval_permission',{p_user:userId,p_can_approve:!!canApprove,p_can_approve_own:!!canApproveOwn});if(r.error)throw r.error},async submit({module,entityType,entityId,actionType,summary,amount=null,payload={}}){const r=await supabase.rpc('submit_approval_request',{p_module:module,p_entity_type:entityType,p_entity_id:String(entityId),p_action_type:actionType,p_summary:summary,p_amount:amount,p_payload:payload});if(r.error)throw r.error;return r.data},async submitPayment(estimateId,status,amount,requestKey){const r=await supabase.rpc('submit_payment_for_approval',{p_estimate:estimateId,p_status:status,p_amount:Number(amount),p_request_key:requestKey});if(r.error)throw r.error;return r.data}};
