import{requireBackend}from'./supabase';import{assertDealerSession}from'./dealerSession';
// Public repair requests become dealer-visible only after TORVO routes them.
const STATES=Object.freeze(['routed','accepted','closed']);const id=v=>{const s=String(v||'').trim();if(!s)throw new Error('REPAIR REQUIREMENT REQUIRED');if(s.length>128)throw new Error('REPAIR REQUIREMENT IS INVALID');return s};const state=(v,allowAll=false)=>{if((v==null||v==='')&&allowAll)return null;const s=String(v||'').trim().toLowerCase();if(!STATES.includes(s))throw new Error('INVALID REPAIR STATUS');return s};const proof=async()=>{const session=await assertDealerSession();if(!session?.deviceId||!session?.token)throw new Error('ACTIVE DEALER DEVICE SESSION REQUIRED');return session};const rpc=async(name,args)=>{const session=await proof();const{data,error}=await requireBackend().rpc(name,{...args,p_device_id:session.deviceId,p_session_token:session.token});if(error)throw error;await assertDealerSession();return data};
export const loadDealerRepairRequirements=async(status=null,limit=100)=>{const expected=state(status,true),n=Math.max(1,Math.min(Math.trunc(Number(limit)||100),500)),data=await rpc('dealer_repair_requirements',{p_status:expected,p_limit:n});if(data==null)return[];if(!Array.isArray(data))throw new Error('INVALID DEALER REPAIR RESPONSE');const seen=new Set();return data.filter(r=>{if(!r||typeof r!=='object')return false;const key=String(r.requirement_id||r.id||'').trim(),s=String(r.status||'').trim().toLowerCase();if(!key||seen.has(key)||!STATES.includes(s)||(expected&&s!==expected))return false;seen.add(key);return true}).slice(0,n)};
export const updateDealerRepairRequirement=async(requirementId,status)=>{const requirement=id(requirementId),next=state(status);if(!['accepted','closed'].includes(next))throw new Error('REPAIR STATUS MUST BE ACCEPTED OR CLOSED');const data=await rpc('dealer_update_repair_requirement',{p_requirement_id:requirement,p_status:next});if(data!==true)throw new Error('REPAIR UPDATE NOT CONFIRMED');return{requirement_id:requirement,status:next}};
export const acceptDealerRepairRequirement=requirementId=>updateDealerRepairRequirement(requirementId,'accepted');export const closeDealerRepairRequirement=requirementId=>updateDealerRepairRequirement(requirementId,'closed');export const loadOpenDealerRepairRequirements=limit=>loadDealerRepairRequirements('routed',limit);export const loadAcceptedDealerRepairRequirements=limit=>loadDealerRepairRequirements('accepted',limit);export const loadClosedDealerRepairRequirements=limit=>loadDealerRepairRequirements('closed',limit);export const dealerRepairStatuses=STATES;
