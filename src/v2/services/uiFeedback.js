let audioContext;
const KEY='torvo-v2-ui-sound';
const getContext=()=>{if(typeof window==='undefined')return null;const Ctx=window.AudioContext||window.webkitAudioContext;if(!Ctx)return null;if(!audioContext)audioContext=new Ctx();return audioContext};
export const isUiSoundEnabled=()=>{if(typeof window==='undefined')return true;return window.localStorage.getItem(KEY)!=='off'};
export const setUiSoundEnabled=enabled=>{if(typeof window!=='undefined')window.localStorage.setItem(KEY,enabled?'on':'off');return enabled};
const tone=(frequency=520,duration=.045,volume=.018,type='sine')=>{if(!isUiSoundEnabled())return;try{const ctx=getContext();if(!ctx)return;if(ctx.state==='suspended')ctx.resume();const osc=ctx.createOscillator(),gain=ctx.createGain(),now=ctx.currentTime;osc.type=type;osc.frequency.setValueAtTime(frequency,now);gain.gain.setValueAtTime(.0001,now);gain.gain.exponentialRampToValueAtTime(volume,now+.006);gain.gain.exponentialRampToValueAtTime(.0001,now+duration);osc.connect(gain);gain.connect(ctx.destination);osc.start(now);osc.stop(now+duration+.01)}catch{}}
export const uiSound={tap:()=>tone(540,.035,.012),open:()=>tone(650,.055,.014),success:()=>{tone(620,.055,.015);setTimeout(()=>tone(820,.07,.014),55)},warning:()=>tone(310,.09,.014,'triangle'),error:()=>tone(210,.11,.016,'triangle')};
export const installGlobalUiFeedback=()=>{if(typeof document==='undefined')return()=>{};const handler=e=>{const target=e.target.closest('button,[role="button"],a');if(!target||target.disabled||target.getAttribute('aria-disabled')==='true'||target.dataset.silent==='true')return;uiSound.tap()};document.addEventListener('click',handler,{passive:true});return()=>document.removeEventListener('click',handler)};
