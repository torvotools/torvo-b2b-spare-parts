import{requireBackend}from'./supabase';
const digits=v=>String(v||'').replace(/\D/g,'').slice(-10);const STAFF=new Set(['owner','admin','accountant','salesman','store_keeper']);const SESSION_KEY='torvo_staff_session_id';
const codeDigits=(v,n)=>String(v||'').replace(/\D/g,'').slice(0,n);const assertDevice=v=>{const x=String(v||'').trim();if(!x||x.length>180)throw new Error('SECURE DEVICE ID REQUIRED.');return x};
export async function identifyLoginRole(mobile){const client=requireBackend();const m=digits(mobile);if(m.length!==10)throw new Error('ENTER A VALID 10-DIGIT MOBILE NUMBER.');const{data,error}=await client.rpc('public_login_route',{p_mobile:m});if(error)throw error;return data}
export async function requestStaffOtp(mobile){const client=requireBackend();const m=digits(mobile);if(m.length!==10)throw new Error('ENTER A VALID 10-DIGIT MOBILE NUMBER.');const{data,error}=await client.functions.invoke('staff-whatsapp-otp-request',{body:{mobile:m}});if(error)throw error;return data}
function keep(result){if(result?.session_id)localStorage.setItem(SESSION_KEY,result.session_id);return result}
export async function verifyStaffOtp(mobile,otp,deviceId){const client=requireBackend(),m=digits(mobile),o=codeDigits(otp,6);if(m.length!==10||o.length!==6)throw new Error('ENTER THE 6-DIGIT WHATSAPP OTP.');const{data,error}=await client.functions.invoke('staff-whatsapp-otp-verify',{body:{mobile:m,otp:o,device_id:assertDevice(deviceId)}});if(error)throw error;return keep(data)}
export async function verifyEmergencyCode(mobile,code,deviceId){const client=requireBackend(),m=digits(mobile),c=codeDigits(code,10);if(m.length!==10||c.length<6)throw new Error('ENTER THE 6 TO 10-DIGIT ADMIN CODE.');const{data,error}=await client.functions.invoke('staff-emergency-login',{body:{mobile:m,code:c,device_id:assertDevice(deviceId)}});if(error)throw error;return keep(data)}
export async function verifyDealerPin(mobile,pin,deviceId){const client=requireBackend();const m=digits(mobile),p=codeDigits(pin,4);if(m.length!==10||p.length!==4)throw new Error('ENTER A VALID 4-DIGIT PIN.');const{data,error}=await client.functions.invoke('dealer-pin-login',{body:{mobile:m,pin:p,device_id:assertDevice(deviceId)}});if(error)throw error;return data}
export async function requestDealerPinRecovery(mobile){const client=requireBackend();const m=digits(mobile);if(m.length!==10)throw new Error('ENTER A VALID 10-DIGIT MOBILE NUMBER.');const{data,error}=await client.functions.invoke('dealer-pin-recovery-request',{body:{mobile:m}});if(error)throw error;return data}
export function isStaffRole(role){return STAFF.has(role)}
export function deviceId(){let id=localStorage.getItem('torvo_device_id');if(!id){id=crypto.randomUUID();localStorage.setItem('torvo_device_id',id)}return id}
