import{forceSignOut}from'./auth';import{requireBackend}from'./supabase';import{clearDealerSession,dealerSessionToken,deviceId}from'./staffAuth';
export async function enforceDealerSession(user){if(!user?.active||String(user.role||'').toLowerCase()!=='dealer')return true;const token=dealerSessionToken();if(!token){await forceSignOut();return false}const client=requireBackend();const{data,error}=await client.functions.invoke('dealer-session-valid',{body:{device_id:deviceId(),dealer_session_token:token}});if(error||!data?.ok){clearDealerSession();await forceSignOut();return false}return true}
