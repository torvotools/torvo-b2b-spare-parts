import{requireBackend}from'./supabase';import{assertDealerSession}from'./dealerSession';
const requiredId=(v,label='ITEM')=>{const id=String(v||'').trim();if(!id)throw new Error(`${label} ID IS REQUIRED`);if(id.length>128)throw new Error(`${label} ID IS INVALID`);return id};
const call=async(name,args)=>{const proof=await assertDealerSession();if(!proof?.deviceId||!proof?.token)throw new Error('DEALER DEVICE SESSION REQUIRED');const{data,error}=await requireBackend().rpc(name,{...args,p_device_id:proof.deviceId,p_session_token:proof.token});if(error)throw error;await assertDealerSession();return data};
const qty=v=>{const n=Number(v);if(!Number.isInteger(n)||n<1||n>9999)throw new Error('QUANTITY MUST BE INTEGER 1 TO 9999');return n};
export const dealerItemRate=(itemId,quantity)=>call('dealer_item_rate',{p_item:requiredId(itemId),p_qty:qty(quantity)});
export const submitDealerPurchaseOrder=lines=>{if(!Array.isArray(lines))throw new Error('PURCHASE ORDER LINES ARE REQUIRED');if(lines.length>500)throw new Error('PURCHASE ORDER CANNOT EXCEED 500 ITEMS');const clean=lines.map(x=>({item_id:requiredId(x?.item_id),qty:qty(x?.qty)}));if(!clean.length)throw new Error('PURCHASE ORDER REQUIRES AT LEAST ONE ITEM');if(new Set(clean.map(x=>x.item_id)).size!==clean.length)throw new Error('DUPLICATE ITEM IS NOT ALLOWED');return call('submit_purchase_order',{p_lines:clean})};
