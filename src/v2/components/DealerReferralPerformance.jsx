import React,{useEffect,useMemo,useState}from'react';import{BarChart3,MessageCircle,MousePointerClick,Phone,RefreshCw,Store,TrendingUp}from'lucide-react';import{requireBackend}from'../services/supabase';
const db=()=>requireBackend(),n=v=>Number(v||0);export default function DealerReferralPerformance(){const[rows,setRows]=useState([]),[busy,setBusy]=useState(true),[error,setError]=useState('');const load=async()=>{setBusy(true);setError('');const to=new Date(),from=new Date(to);from.setDate(from.getDate()-30);const{data,error:e}=await db().rpc('admin_dealer_referral_performance',{p_from:from.toISOString(),p_to:to.toISOString()});if(e)setError(String(e.message||e).toUpperCase());else setRows(data||[]);setBusy(false)};useEffect(()=>{load()},[]);const total=useMemo(()=>rows.reduce((a,r)=>({customers:a.customers+n(r.total_customers),calls:a.calls+n(r.call_clicks),whatsapp:a.whatsapp+n(r.whatsapp_clicks),converted:a.converted+n(r.confirmed_conversions)}),{customers:0,calls:0,whatsapp:0,converted:0}),[rows]);return <section className="panel workspace"><div className="workspaceHead"><div><span className="eyebrow">LAST 30 DAYS</span><h2>DEALER REFERRAL PERFORMANCE</h2><p>WEBSITE CUSTOMER REFERRALS AND CONTACT ACTIONS. CALL / WHATSAPP CLICKS ARE NOT COUNTED AS SALES.</p></div><button onClick={load} disabled={busy}><RefreshCw size={16}/>{busy?' LOADING…':' REFRESH'}</button></div><div className="statGrid"><article><Store/><span>TOTAL CUSTOMERS REFERRED</span><b>{total.customers}</b></article><article><Phone/><span>CALL CLICKS</span><b>{total.calls}</b></article><article><MessageCircle/><span>WHATSAPP CLICKS</span><b>{total.whatsapp}</b></article><article><TrendingUp/><span>CONFIRMED CONVERSIONS</span><b>{total.converted}</b></article></div>{error?<div className="empty"><p>{error}</p></div>:<div className="tableWrap"><table><thead><tr><th>DEALER</th><th>CUSTOMERS</th><th>PROFILE VIEWS</th><th>SELECTED</th><th>CALL</th><th>WHATSAPP</th><th>DIRECTIONS</th><th>CONFIRMED</th></tr></thead><tbody>{rows.map(r=><tr key={r.dealer_id}><td><b>{r.shop_name}</b></td><td>{n(r.total_customers)}</td><td>{n(r.profile_views)}</td><td>{n(r.dealer_selections)}</td><td>{n(r.call_clicks)}</td><td>{n(r.whatsapp_clicks)}</td><td>{n(r.directions_clicks)}</td><td><b>{n(r.confirmed_conversions)}</b></td></tr>)}{!busy&&!rows.length&&<tr><td colSpan="8"><div className="empty"><BarChart3/><p>NO REFERRAL ACTIVITY IN THE LAST 30 DAYS.</p></div></td></tr>}</tbody></table></div>}<div className="instruction"><MousePointerClick size={16}/><span>PROFILE VIEW, CALL, WHATSAPP AND DIRECTIONS ARE CUSTOMER ACTIONS. ONLY AN AUTHORIZED CONFIRMATION MAY BE RECORDED AS A CONVERSION.</span></div></section>}
