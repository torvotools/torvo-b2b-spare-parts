import React,{useMemo,useRef,useState}from'react';
import{AlertCircle,CheckCircle2,ShoppingCart,Trash2,X}from'lucide-react';
import{secureDealerLegacy as data}from'../services/dealerLegacyBridge';
import DealerCatalogItemFinder from'./DealerCatalogItemFinder';
const money=n=>`₹${Number(n||0).toLocaleString('en-IN',{maximumFractionDigits:2})}`;
const rid=r=>String(r?.id||r?.request_id||'').trim();
export default function DealerAddOnOrderModal({request,onClose,onCreated}){
 const[items,setItems]=useState([]),[cart,setCart]=useState({}),[rates,setRates]=useState({}),[busy,setBusy]=useState(false),[err,setErr]=useState(''),[confirming,setConfirming]=useState(false);const rateVersion=useRef({});
 const selected=useMemo(()=>items.filter(x=>Number.isInteger(Number(cart[x.id]))&&Number(cart[x.id])>=1&&Number(cart[x.id])<=9999),[items,cart]);const total=selected.reduce((n,x)=>n+Number(rates[x.id]?.amount||0),0);const bad=selected.some(x=>!rates[x.id]?.rate||rates[x.id]?.loading||rates[x.id]?.error);
 const add=x=>{if(!x?.id)return;setItems(v=>v.some(i=>i.id===x.id)?v:[...v,x]);changeQty(x.id,1)};
 const remove=id=>{rateVersion.current[id]=(rateVersion.current[id]||0)+1;setItems(v=>v.filter(x=>x.id!==id));setCart(v=>{const n={...v};delete n[id];return n});setRates(v=>{const n={...v};delete n[id];return n})};
 const changeQty=async(id,value)=>{if(!id)return;const raw=String(value??'').trim();if(raw===''){rateVersion.current[id]=(rateVersion.current[id]||0)+1;setCart(v=>({...v,[id]:''}));setRates(v=>{const n={...v};delete n[id];return n});return}const num=Number(raw);if(!Number.isInteger(num)||num<1||num>9999){rateVersion.current[id]=(rateVersion.current[id]||0)+1;setCart(v=>({...v,[id]:raw}));setRates(v=>({...v,[id]:{error:'QUANTITY MUST BE INTEGER 1 TO 9999'}}));return}const version=(rateVersion.current[id]||0)+1;rateVersion.current[id]=version;setCart(v=>({...v,[id]:num}));setRates(v=>({...v,[id]:{loading:true,qty:num}}));try{const rawRate=await data.dealerItemRate(id,num);const rate=Array.isArray(rawRate)?rawRate[0]:rawRate;if(rateVersion.current[id]===version)setRates(v=>({...v,[id]:{...rate,qty:num}}))}catch(e){if(rateVersion.current[id]===version)setRates(v=>({...v,[id]:{error:e?.message||'RATE COULD NOT BE LOADED',qty:num}}))}};
 const create=async()=>{const requestId=rid(request);if(busy||!requestId||!selected.length||bad)return;setBusy(true);setErr('');try{const orderId=await data.dealerCreateAddOnOrder(requestId,selected.map(x=>({item_id:x.id,qty:Number(cart[x.id])})));if(!orderId)throw new Error('ADDITIONAL ORDER WAS NOT CONFIRMED');setConfirming(false);onCreated?.(orderId)}catch(e){setErr(e?.message||'ADDITIONAL ORDER COULD NOT BE CREATED');setConfirming(false)}finally{setBusy(false)}};
 return <div className="modalLayer"><section className="dealerActionModal addOnOrderModal"><header><div><span className="eyebrow">TORVO APPROVED · ADD MORE ITEMS</span><h3>CREATE ADDITIONAL PURCHASE ORDER</h3><p>THIS CREATES A NEW LINKED ORDER. YOUR ORIGINAL PURCHASE ORDER / ESTIMATE REMAINS UNCHANGED.</p></div><button type="button" className="iconBtn" disabled={busy} onClick={onClose} aria-label="CLOSE"><X/></button></header>{err&&<div className="inlineError" role="alert"><AlertCircle size={16}/>{err}</div>}<div className="addOnApproval"><CheckCircle2/><div><b>APPROVAL ACTIVE</b><span>{request?.admin_note||'TORVO HAS APPROVED YOUR ADD MORE ITEMS REQUEST.'}</span></div></div><DealerCatalogItemFinder selectedIds={items.map(x=>x.id)} onChoose={add}/><div className="addOnCatalog selectedAddOnItems">{!items.length?<div className="miniEmpty">FIND AND ADD PRODUCTS ABOVE.</div>:items.map(x=>{const r=rates[x.id];return <article key={x.id}><div><b>{x.item_code}</b><strong>{x.name}</strong><small>{[x.oem_code&&`OEM ${x.oem_code}`,x.brand,x.category].filter(Boolean).join(' · ')}</small>{r?.loading&&<span className="dealerRate">CHECKING YOUR RATE…</span>}{r?.rate&&!r.loading&&<span className="dealerRate">YOUR RATE: <strong>{money(r.rate)}</strong> · AMOUNT: <strong>{money(r.amount)}</strong></span>}{r?.error&&<span className="dealerRate error">{r.error}</span>}</div><input aria-label={`QUANTITY FOR ${x.name||x.item_code}`} type="number" inputMode="numeric" min="1" max="9999" step="1" value={cart[x.id]??''} onChange={e=>changeQty(x.id,e.target.value)} placeholder="QTY"/><button type="button" className="iconBtn" disabled={busy} onClick={()=>remove(x.id)} aria-label={`REMOVE ${x.name||x.item_code}`}><Trash2 size={15}/></button></article>})}</div>{selected.length>0&&<div className="poSummary"><div><strong>{selected.length} ITEM{selected.length===1?'':'S'} SELECTED</strong><span>{bad?'WAITING FOR VALID DEALER RATES':'ADDITIONAL PURCHASE ORDER TOTAL'}</span></div><b>{money(total)}</b></div>}<div className="modalNotice"><strong>RATE SECURITY:</strong> DISPLAYED RATES ARE ONLY A PREVIEW. ON FINAL SUBMIT, TORVO RECALCULATES EVERY RATE ON THE SERVER FROM YOUR CURRENT DEALER RATE GROUP AND QUANTITY. EXISTING ORDER TOTALS ARE NEVER OVERWRITTEN.</div><footer><button type="button" className="secondary" disabled={busy} onClick={onClose}>CANCEL</button><button type="button" className="primary" disabled={busy||!rid(request)||!selected.length||bad} onClick={()=>setConfirming(true)}><ShoppingCart/>{bad?'CHECKING RATES…':'REVIEW & CREATE'}</button></footer></section>{confirming&&<div className="modalLayer nestedConfirm"><section className="dealerActionModal compactConfirm"><header><div><span className="eyebrow">FINAL CHECK</span><h3>CREATE ADDITIONAL PURCHASE ORDER?</h3></div><button type="button" className="iconBtn" disabled={busy} onClick={()=>setConfirming(false)} aria-label="CLOSE FINAL CHECK"><X size={17}/></button></header><p>{selected.length} ITEM{selected.length===1?'':'S'} · PREVIEW TOTAL <strong>{money(total)}</strong>. TORVO WILL RECALCULATE RATES SECURELY ON SUBMIT.</p><div className="modalNotice">A NEW LINKED SALES ORDER WILL BE CREATED. THE ORIGINAL ORDER AND ESTIMATE WILL REMAIN UNCHANGED.</div><footer><button type="button" className="secondary" disabled={busy} onClick={()=>setConfirming(false)}>BACK</button><button type="button" className="primary" disabled={busy||bad||!rid(request)} onClick={create}><ShoppingCart size={15}/>{busy?'CREATING…':'CONFIRM NEW ORDER'}</button></footer></section></div>}</div>;
}
