import React from'react';import{PackageCheck,ShieldCheck,X}from'lucide-react';
export default function CatalogSaveConfirmModal({item,title,busy,onClose,onConfirm}){if(!item)return null;return <div className="modalLayer"><div className="dealerActionModal catalogConfirmModal"><header><div><span className="eyebrow">ITEM MASTER CONFIRMATION</span><h3>{item.id?'Update':'Create'} {title}</h3><p>{item.item_code} · {item.name}</p></div><button className="iconBtn" disabled={busy} onClick={onClose}><X size={18}/></button></header><div className="modalForm"><div className="requirementMiniProgress"><span>BRAND <b>{item.brand||'—'}</b></span><span>CATEGORY <b>{item.category||'—'}</b></span><span>MODEL <b>{item.model||'—'}</b></span></div><div className="modalNotice"><ShieldCheck size={14}/>Confirm the Item Master details. Duplicate item codes remain blocked before save.</div></div><footer><button className="secondary" disabled={busy} onClick={onClose}>CANCEL</button><button className="primary" disabled={busy} onClick={onConfirm}><PackageCheck size={15}/>{busy?'SAVING…':item.id?'UPDATE ITEM':'CREATE ITEM'}</button></footer></div></div>}
