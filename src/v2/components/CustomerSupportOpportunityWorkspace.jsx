import React,{useEffect,useMemo,useState}from'react';import{AlertTriangle,PackageSearch,RefreshCw,Search,CheckCircle2}from'lucide-react';import{requireBackend}from'../services/supabase';
const db=()=>requireBackend(),REQ=['OPEN','UNDER REVIEW','PRODUCT ADDED','FULFILLED','CLOSED'],COMP=['OPEN','UNDER REVIEW','RESOLVED','REJECTED'];
export default function CustomerSupportOpportunityWorkspace(){const[tab,setTab]=useState('requirements'),[requirements,setRequirements]=useState([]),[complaints,setComplaints]=useState([]),[busy,setBusy]=useState(false),[error,setError]=useState(''),[notice,setNotice]=useState(''),[query,setQuery]=useState(''),[status,setStatus]=useState('ALL'),[productIds,setProductIds]=useState({}),[notes,setNotes]=useState({});const load=async()=>{setBusy(true);setError('');try{const[a,b]=await Promise.all([db().rpc('torvo_admin_customer_requirements',{p_status:null}),db().rpc('torvo_admin_customer_complaints',{p_status:null})]);if(a.error)throw a.error;if(b.error)throw b.error;setRequirements(a.data||[]);setComplaints(b.data||[])}catch(e){setError(String(e?.message||e).toUpperCase())}finally{setBusy(false)}};useEffect(()=>{load()},[]);const filtered=useMemo(()=>{const list=tab==='requirements'?requirements:complaints,q=query.trim().toUpperCase();return list.filter(x=>(status==='ALL'||x.status===status)&&(!q||JSON.stringify(x).toUpperCase().includes(q)))},[tab,requirements,complaints,query,status]);const updateRequirement=async(r,next)=>{const productId=(productIds[r.id]||r.linked_product_id||'').trim()||null;if(['PRODUCT ADDED','FULFILLED'].includes(next)&&!productId)return setError('LINK A VALID CATALOG PRODUCT ID BEFORE PRODUCT ADDED / FULFILLED');setBusy(true);setError('');setNotice('');try{const{error:e}=await db().rpc('torvo_admin_update_customer_requirement',{p_requirement_id:r.id,p_status:next,p_linked_product_id:productId});if(e)throw e;setNotice('REQUIREMENT UPDATED');await load()}catch(e){setError(String(e?.message||e).toUpperCase());setBusy(false)}};const updateComplaint=async(c,next)=>{const note=(notes[c.id]||c.resolution_note||'').trim();if(['RESOLVED','REJECTED'].includes(next)&&note.length<3)return setError('ADD A RESOLUTION NOTE BEFORE RESOLVED / REJECTED');setBusy(true);setError('');setNotice('');try{const{error:e}=await db().rpc('torvo_admin_update_customer_complaint',{p_complaint_id:c.id,p_status:next,p_resolution_note:note||null});if(e)throw e;setNotice('COMPLAINT UPDATED');await load()}catch(e){setError(String(e?.message||e).toUpperCase());setBusy(false)}};const statuses=tab==='requirements'?REQ:COMP;return <section className="panel workspace"><div className="workspaceHead"><div><span className="eyebrow">CUSTOMER DEMAND + SUPPORT</span><h2>OPPORTUNITY & COMPLAINT CENTER</h2><p>MISSING RANGE IS CONVERTED INTO CATALOG OPPORTUNITY; COMPLAINTS REMAIN REVIEWED ADMIN CASES.</p></div><button onClick={load} disabled={busy}><RefreshCw size={16}/>{busy?' LOADING…':' REFRESH'}</button></div><div className="workspaceTabs"><button className={tab==='requirements'?'active':''} onClick={()=>{setTab('requirements');setStatus('ALL')}}>MISSING RANGE ({requirements.length})</button><button className={tab==='complaints'?'active':''} onClick={()=>{setTab('complaints');setStatus('ALL')}}>COMPLAINTS ({complaints.length})</button></div><div className="workspaceFilters"><label><Search size={15}/><input value={query} onChange={e=>setQuery(e.target.value.toUpperCase())} placeholder="SEARCH CUSTOMER / MOBILE / BRAND / MODEL / ITEM"/></label><select value={status} onChange={e=>setStatus(e.target.value)}><option>ALL</option>{statuses.map(s=><option key={s}>{s}</option>)}</select></div>{notice&&<div className="dealerResultNotice"><CheckCircle2/><span><b>{notice}</b></span></div>}{error&&<div className="empty"><p>{error}</p></div>}{!error&&tab==='requirements'&&<div className="tableWrap"><table><thead><tr><th>DATE</th><th>CUSTOMER / AREA</th><th>REQUIREMENT</th><th>DETAIL</th><th>CATALOG LINK</th><th>WORKFLOW</th></tr></thead><tbody>{filtered.map(r=><tr key={r.id}><td>{new Date(r.created_at).toLocaleDateString()}</td><td><b>{r.customer_name}</b><br/>{r.mobile_whatsapp}<br/><small>{[r.city,r.district,r.state,r.pin_code].filter(Boolean).join(' · ')}</small></td><td><b>{r.required_item}</b><br/><small>{r.product_type} · QTY {r.quantity}</small></td><td>{[r.brand,r.machine_model,r.item_oem_no].filter(Boolean).join(' · ')||'—'}{r.description&&<><br/><small>{r.description}</small></>}</td><td><input value={productIds[r.id]??r.linked_product_id??''} onChange={e=>setProductIds(v=>({...v,[r.id]:e.target.value.trim()}))} placeholder="CATALOG PRODUCT UUID"/><small>{r.linked_product_id?'LINKED':'LINK WHEN PRODUCT IS ADDED'}</small></td><td><select value={r.status} disabled={busy} onChange={e=>updateRequirement(r,e.target.value)}>{REQ.map(s=><option key={s}>{s}</option>)}</select></td></tr>)}{!busy&&!filtered.length&&<tr><td colSpan="6"><div className="empty"><PackageSearch/><p>NO MATCHING PRODUCT REQUIREMENTS.</p></div></td></tr>}</tbody></table></div>}{!error&&tab==='complaints'&&<div className="tableWrap"><table><thead><tr><th>DATE</th><th>CUSTOMER</th><th>CATEGORY / DETAILS</th><th>DEALER / ENQUIRY</th><th>RESOLUTION NOTE</th><th>WORKFLOW</th></tr></thead><tbody>{filtered.map(c=><tr key={c.id}><td>{new Date(c.created_at).toLocaleDateString()}</td><td><b>{c.customer_name}</b><br/>{c.mobile_whatsapp}</td><td><b>{c.category}</b><br/><small>{c.description}</small></td><td>{c.dealer_id||'—'}<br/><small>{c.enquiry_id?`ENQUIRY ${c.enquiry_id}`:'NO LINKED ENQUIRY'}</small></td><td><textarea value={notes[c.id]??c.resolution_note??''} onChange={e=>setNotes(v=>({...v,[c.id]:e.target.value.toUpperCase()}))} placeholder="ADMIN REVIEW / RESOLUTION NOTE"/></td><td><select value={c.status} disabled={busy} onChange={e=>updateComplaint(c,e.target.value)}>{COMP.map(s=><option key={s}>{s}</option>)}</select></td></tr>)}{!busy&&!filtered.length&&<tr><td colSpan="6"><div className="empty"><AlertTriangle/><p>NO MATCHING CUSTOMER COMPLAINTS.</p></div></td></tr>}</tbody></table></div>}</section>}
