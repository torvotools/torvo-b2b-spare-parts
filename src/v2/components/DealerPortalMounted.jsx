import React,{useEffect,useState}from'react';import{ShoppingCart,QrCode,Wrench}from'lucide-react';import DealerPortal from'./DealerPortal';import DealerApprovedAddOnOrders from'./DealerApprovedAddOnOrders';import DealerReferralWorkspace from'./DealerReferralWorkspace';import DealerRepairWorkspace from'./DealerRepairWorkspace';import DealerMissingPartForm from'./DealerMissingPartForm';import{assertDealerSession}from'../services/dealerSession';
export default function DealerPortalMounted({user}){const[mode,setMode]=useState('business'),[ready,setReady]=useState(false),[err,setErr]=useState('');const linked=user?.role==='dealer'&&Boolean(user?.dealer_id);useEffect(()=>{let live=true;if(!linked){setReady(false);return()=>{live=false}}setReady(false);setErr('');assertDealerSession().then(()=>{if(live)setReady(true)}).catch(e=>{if(live)setErr(e?.message||'DEALER SESSION ENDED')});return()=>{live=false}},[linked,user?.dealer_id]);if(!linked)return <DealerPortal user={user||{}}/>;if(err)return <section className="panel workspace"><div className="empty"><h3>DEALER SESSION ENDED</h3><p>{err}</p><p>PLEASE LOGIN AGAIN ON THE CURRENT APPROVED DEVICE.</p></div></section>;if(!ready)return <section className="panel workspace"><div className="empty"><h3>VERIFYING DEALER DEVICE</h3><p>CHECKING YOUR CURRENT APPROVED MOBILE SESSION…</p></div></section>;return <><div className="workspaceTabs quickActions" role="tablist" aria-label="DEALER WORKSPACE"><button type="button" role="tab" aria-selected={mode==='business'} className={mode==='business'?'active':''} onClick={()=>setMode('business')}><ShoppingCart size={15}/>DEALER BUSINESS</button><button type="button" role="tab" aria-selected={mode==='referral'} className={mode==='referral'?'active':''} onClick={()=>setMode('referral')}><QrCode size={15}/>CUSTOMER REFERRAL</button><button type="button" role="tab" aria-selected={mode==='repair'} className={mode==='repair'?'active':''} onClick={()=>setMode('repair')}><Wrench size={15}/>REPAIR & SERVICE</button></div>{mode==='business'&&<><DealerPortal user={user}/><DealerMissingPartForm/><DealerApprovedAddOnOrders/></>}{mode==='referral'&&<DealerReferralWorkspace/>}{mode==='repair'&&<DealerRepairWorkspace/>}</>}
