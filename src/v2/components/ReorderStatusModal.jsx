import React from'react';import{CheckCircle2,ShieldCheck,Trash2,X}from'lucide-react';
export default function ReorderStatusModal({request,status,busy,onClose,onConfirm}){if(!request||!status)return null;const cancel=status==='cancelled';return <div className="modalLayer"><div className="dealerActionModal inventoryActionModal"><header><div><span className={`status ${cancel?'rejected':'approved'}`}>{cancel?'CANCEL':'ORDERED'}</span><h3>{cancel?'Cancel Reorder Request':'Mark Reorder as Ordered'}</h3><p>{request.catalog_items?.item_code||'ITEM'} · {request.catalog_items?.name||'Catalog item'}</p></div><button className="iconBtn" disabled={busy} onClick={onClose}><X size={18}/></button></header><div className="modalForm"><div className="requirementMiniProgress"><span>REQUIRED <b>{request.required_qty}</b></span><span>RECEIVED <b>{request.received_qty||0}</b></span></div><div className="modalNotice"><ShieldCheck size={14}/>{cancel?'The planning request will be cancelled. Inventory stock will not change.':'This records ordering status only. Actual supplier receipt must still be entered through Purchase Entry.'}</div></div><footer><button className="secondary" disabled={busy} onClick={onClose}>BACK</button><button className={cancel?'danger primary':'primary'} disabled={busy} onClick={onConfirm}>{cancel?<Trash2 size={15}/>:<CheckCircle2 size={15}/>} {busy?'SAVING…':cancel?'CONFIRM CANCEL':'MARK ORDERED'}</button></footer></div></div>}
