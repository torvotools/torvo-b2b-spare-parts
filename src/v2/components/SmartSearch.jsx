import React,{useMemo,useRef,useState}from'react';import{Search,Mic,Camera,X,Package,Cog,Boxes}from'lucide-react';
const norm=v=>String(v||'').trim().toLowerCase();
const iconFor=t=>t==='machine'?Cog:t==='accessory'?Boxes:Package;
export default function SmartSearch({value,onChange,items=[],placeholder='SEARCH ITEM CODE, OEM CODE, PRODUCT, BRAND, CATEGORY OR MODEL…',onCamera,onMic,maxSuggestions=8}){const[focused,setFocused]=useState(false),[active,setActive]=useState(-1),inputRef=useRef(null),terms=useMemo(()=>norm(value).split(/\s+/).filter(Boolean),[value]);const suggestions=useMemo(()=>{if(!terms.length)return[];return items.filter(x=>{const hay=norm(`${x.item_code||''} ${x.oem_code||''} ${x.company_code||''} ${x.original_code||''} ${x.name||''} ${x.brand||''} ${x.category||''} ${x.model||''} ${x.item_type||''}`);return terms.every(t=>hay.includes(t))}).slice(0,maxSuggestions)},[items,terms,maxSuggestions]);const choose=x=>{onChange([x.name,x.model].filter(Boolean).join(' '));setFocused(false);setActive(-1);inputRef.current?.focus()};const keyDown=e=>{if(e.key==='ArrowDown'&&suggestions.length){e.preventDefault();setFocused(true);setActive(i=>(i+1)%suggestions.length)}else if(e.key==='ArrowUp'&&suggestions.length){e.preventDefault();setFocused(true);setActive(i=>(i<=0?suggestions.length-1:i-1))}else if(e.key==='Enter'&&active>=0&&suggestions[active]){e.preventDefault();choose(suggestions[active])}else if(e.key==='Escape'){setFocused(false);setActive(-1)}};return <div className="smartSearchWrap"><div className="smartSearch" role="search"><Search size={20}/><input ref={inputRef} value={value} onChange={e=>{onChange(e.target.value);setActive(-1)}} onFocus={()=>setFocused(true)} onBlur={()=>setTimeout(()=>setFocused(false),120)} onKeyDown={keyDown} placeholder={placeholder} autoComplete="off" role="combobox" aria-autocomplete="list" aria-expanded={focused&&terms.length>0} aria-controls="smart-search-list" aria-activedescendant={active>=0?`smart-option-${active}`:undefined}/>{value&&<button onMouseDown={e=>e.preventDefault()} onClick={()=>{onChange('');setActive(-1)}} aria-label="CLEAR SEARCH"><X size={17}/></button>}<button className="smartMedia" onMouseDown={e=>e.preventDefault()} onClick={()=>onCamera?.()} aria-label="CAMERA SEARCH" title="CAMERA SEARCH"><Camera size={19}/></button><button className="smartMedia" onMouseDown={e=>e.preventDefault()} onClick={()=>onMic?.()} aria-label="VOICE SEARCH" title="VOICE SEARCH"><Mic size={19}/></button></div>{focused&&terms.length>0&&<div className="smartSuggestions" id="smart-search-list" role="listbox">{suggestions.length?suggestions.map((x,i)=>{const I=iconFor(x.item_type);return <button type="button" id={`smart-option-${i}`} role="option" aria-selected={i===active} className={i===active?'active':''} key={x.id||`${x.item_code}-${x.model}`} onMouseDown={e=>e.preventDefault()} onMouseEnter={()=>setActive(i)} onClick={()=>choose(x)}><span className="smartThumb">{x.image_url?<img src={x.image_url} alt=""/>:<I size={18}/>}</span><span><strong>{x.name||x.item_code}</strong><small>{[x.item_code,x.oem_code||x.company_code||x.original_code,x.brand,x.category,x.model,x.item_type?.replace('_',' ')].filter(Boolean).join(' · ')}</small></span><Search size={15}/></button>}):<div className="smartNoMatch">NO MATCHING PRODUCT YET</div>}</div>}</div>}
