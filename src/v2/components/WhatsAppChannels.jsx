import React,{useState}from'react';import{MessageCircle}from'lucide-react';import{openWhatsAppChannel}from'../services/whatsappChannels';
export default function WhatsAppChannels({dual=false,context='TORVO BUSINESS SUPPORT'}){const[err,setErr]=useState('');const open=async kind=>{setErr('');try{await openWhatsAppChannel(kind,`HELLO TORVO · ${context}`)}catch(e){setErr(e.message)}};return <div className="whatsappChannels">{dual&&<button type="button" className="iconBtn" title="CUSTOMER WHATSAPP" aria-label="CUSTOMER WHATSAPP" onClick={()=>open('customer')}><MessageCircle size={18}/><span>CUSTOMER</span></button>}<button type="button" className="iconBtn" title="BUSINESS WHATSAPP" aria-label="BUSINESS WHATSAPP" onClick={()=>open('business')}><MessageCircle size={18}/><span>BUSINESS</span></button>{err&&<small className="inlineError">{err}</small>}</div>}
