import React,{useEffect,useState}from'react';import{RefreshCw,Search,PackagePlus,History,ClipboardList}from'lucide-react';import{data}from'../services/repository';
export default function InventoryWorkspace(){const[rows,setRows]=useState([]),[orders,setOrders]=useState([]),[moves,setMoves]=useState([]),[tab,setTab]=useState('stock'),[q,setQ]=useState(''),[err,setErr]=useState('');const load=async()=>{setErr('');try{const[a,b,c]=await Promise.all([data.inventory(),data.reorderRequests(),data.inventoryMovements()]);setRows(a);setOrders(b);setMoves(c)}catch(e){setErr(e.message)}};useEffect(()=>{load()},[]);const adjust=async r=>{const n=Number(prompt('Stock change: use + for add, - for correction','0'));if(!n)return;const reason=prompt('Reason for stock adjustment');if(!reason?.trim())return;try{await data.adjustInventory(r.item_id,n,reason);await load()}catch(e){alert(e.message)}};const reorder=async r=>{const n=Number(prompt('Required / Order Qty','1'));if(!(n>0))return;try{await data.submitReorder(r.item_id,n);await load()}catch(e){alert(e.message)}};const orderAction=async(r,status)=>{try{await data.updateReorderStatus(r.id,status);await load()}catch(e){alert(e.message)}};const receive=async r=>{const n=Number(prompt('Actual received quantity',String(r.required_qty)));if(!(n>0))return;try{await data.receiveReorder(r.id,n);await load()}catch(e){alert(e.message)}};const view=rows.filter(r=>JSON.stringify(r).toLowerCase().includes(q.toLowerCase()));return <section className="panel workspace"><header><div><h3>Inventory Control</h3><p>Stock, Purchase / Order List and movement history. Delivery stock deduction remains separately protected.</p></div><button className="iconBtn" onClick={load}><RefreshCw size={18}/></button></header><div className="workspaceTabs"><button className={tab==='stock'?'active':''} onClick={()=>setTab('stock')}><PackagePlus size={15}/>Current Stock</button><button className={tab==='orders'?'active':''} onClick={()=>setTab('orders')}><ClipboardList size={15}/>Purchase / Order List</button><button className={tab==='moves'?'active':''} onClick={()=>setTab('moves')}><History size={15}/>Movement History</button></div>{err?<div className="empty"><h3>Unable to load</h3><p>{err}</p></div>:tab==='stock'?<><div className="localTools"><label><Search size={16}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search inventory…"/></label></div>{view.length===0?<div className="empty"><h3>No inventory records</h3><p>Catalog inventory will appear here.</p></div>:<div className="requestList">{view.map(r=>{const qty=Number(r.current_qty),level=Number(r.reorder_level),out=qty<=0,low=!out&&qty<=level;return <article key={r.item_id}><div><strong>{r.catalog_items?.item_code} · {r.catalog_items?.name}</strong><span>Stock {r.current_qty} · Reorder {r.reorder_level}</span><small>{[r.catalog_items?.brand,r.catalog_items?.model].filter(Boolean).join(' · ')||r.catalog_items?.item_type}</small></div><b className={`status ${out?'rejected':low?'hold':'approved'}`}>{out?'OUT OF STOCK':low?'LOW':'OK'}</b><div className="rowActions"><button onClick={()=>reorder(r)}><PackagePlus size={15}/>Reorder</button><button onClick={()=>adjust(r)}>Adjust</button></div></article>})}</div>}</>:tab==='orders'?orders.length===0?<div className="empty"><h3>Purchase / Order List empty</h3><p>Reorder requests submitted from Current Stock will appear here.</p></div>:<div className="requestList inventoryList">{orders.map(r=><article key={r.id}><div><strong>{r.catalog_items?.item_code} · {r.catalog_items?.name}</strong><span>Order Qty {r.required_qty}</span><small>{new Date(r.created_at).toLocaleDateString('en-GB')} · {[r.catalog_items?.brand,r.catalog_items?.model].filter(Boolean).join(' · ')}</small></div><b className={`status ${r.status==='received'?'approved':r.status==='cancelled'?'rejected':'pending'}`}>{r.status}</b><div className="rowActions">{r.status==='submitted'&&<><button onClick={()=>orderAction(r,'ordered')}>Mark Ordered</button><button className="danger" onClick={()=>orderAction(r,'cancelled')}>Cancel</button></>}{r.status==='ordered'&&<button className="primary" onClick={()=>receive(r)}>Receive Stock</button>}</div></article>)}</div>:moves.length===0?<div className="empty"><h3>No stock movements</h3><p>Delivery, receiving and manual corrections will appear here.</p></div>:<div className="requestList inventoryList">{moves.map(m=><article key={m.id}><div><strong>{m.catalog_items?.item_code} · {m.catalog_items?.name}</strong><span>{Number(m.qty_change)>0?'+':''}{m.qty_change} · {m.reason}</span><small>{new Date(m.created_at).toLocaleString('en-GB')} · {m.reference_type||'movement'}</small></div><b className={`status ${Number(m.qty_change)>=0?'approved':'rejected'}`}>{Number(m.qty_change)>=0?'IN':'OUT'}</b><div/></article>)}</div>}</section>}
