import React,{useEffect,useMemo,useState}from'react';import{RefreshCw,ShoppingCart,Truck,MessageSquare,SearchX,Gift,WalletCards,SlidersHorizontal,X,Wrench,ChevronRight}from'lucide-react';import{data}from'../services/repository';import SmartSearch from'./SmartSearch';
const money=n=>`₹${Number(n||0).toLocaleString('en-IN')}`;const norm=v=>String(v||'').trim().toLowerCase();const within30=d=>Date.now()-new Date(d).getTime()<=30*86400000;
export default function DealerPortal({user}){const[docs,setDocs]=useState([]),[catalog,setCatalog]=useState([]),[dispatch,setDispatch]=useState([]),[missing,setMissing]=useState([]),[messages,setMessages]=useState([]),[schemes,setSchemes]=useState([]),[rewardTotal,setRewardTotal]=useState(0),[err,setErr]=useState(''),[busy,setBusy]=useState(false),[search,setSearch]=useState(''),[type,setType]=useState(''),[brand,setBrand]=useState(''),[category,setCategory]=useState(''),[model,setModel]=useState(''),[cart,setCart]=useState({}),[spareCart,setSpareCart]=useState({}),[machine,setMachine]=useState(null),[machineSpares,setMachineSpares]=useState([]),[spareBusy,setSpareBusy]=useState(false);const load=async()=>{setErr('');try{const[so,est,m,s,a,dp,nr,msg,sc,balance]=await Promise.all([data.documents('sales_order'),data.documents('estimate'),data.catalog('machine'),data.catalog('spare_part'),data.catalog('accessory'),data.dispatches(),data.nonAvailableRequests(),data.messages(),data.schemes(),data.rewardBalance(user.dealer_id)]);setDocs([...so,...est].filter(d=>within30(d.created_at)).sort((x,y)=>new Date(y.created_at)-new Date(x.created_at)));setCatalog([...m,...s,...a].filter(x=>x.active!==false));setDispatch(dp);setMissing(nr);setMessages(msg);setSchemes(sc);setRewardTotal(balance)}catch(e){setErr(e.message)}};useEffect(()=>{if(user.dealer_id)load()},[user.dealer_id]);const options=(key,base)=>[...new Set(base.map(x=>x[key]).filter(Boolean))].sort((a,b)=>String(a).localeCompare(String(b)));const typeBase=useMemo(()=>catalog.filter(x=>!type||x.item_type===type),[catalog,type]);const brands=useMemo(()=>options('brand',typeBase),[typeBase]);const brandBase=useMemo(()=>typeBase.filter(x=>!brand||x.brand===brand),[typeBase,brand]);const categories=useMemo(()=>options('category',brandBase),[brandBase]);const categoryBase=useMemo(()=>brandBase.filter(x=>!category||x.category===category),[brandBase,category]);const models=useMemo(()=>options('model',categoryBase),[categoryBase]);const filteredBase=useMemo(()=>categoryBase.filter(x=>!model||x.model===model),[categoryBase,model]);const terms=useMemo(()=>norm(search).split(/\s+/).filter(Boolean),[search]);const items=useMemo(()=>filteredBase.filter(x=>{const hay=norm(`${x.item_code} ${x.oem_code||''} ${x.name} ${x.brand||''} ${x.category||''} ${x.model||''} ${x.item_type||''}`);return terms.every(t=>hay.includes(t))}),[filteredBase,terms]);const clearFilters=()=>{setType('');setBrand('');setCategory('');setModel('');setSearch('')};const qty=(id,v)=>setCart(c=>({...c,[id]:v===''?'':Math.max(0,Math.floor(Number(v)||0))}));const spareQty=(id,v)=>setSpareCart(c=>({...c,[id]:v===''?'':Math.max(0,Math.floor(Number(v)||0))}));const openSpares=async m=>{setMachine(m);setMachineSpares([]);setSpareCart({});setSpareBusy(true);try{setMachineSpares(await data.dealerMachineSpares(m.id))}catch(e){alert(e.message);setMachine(null)}finally{setSpareBusy(false)}};const submitPreview=()=>alert('Secure Purchase Order submission with dealer-rate calculation and revision controls is being connected. This preview will not create an incomplete order.');const addMissing=async()=>{const text=prompt('Which product / model do you need?')?.trim();if(!text)return;setBusy(true);try{await data.createNonAvailable(text,null,null);await load()}catch(e){alert(e.message)}finally{setBusy(false)}};const sendMsg=async()=>{const subject=prompt('Message subject (optional)','');const body=prompt('Message to TORVO')?.trim();if(!body)return;setBusy(true);try{await data.sendMessage(user.dealer_id,subject?.trim()||null,body);await load()}catch(e){alert(e.message)}finally{setBusy(false)}};const olderDetails=()=>{const text=`Hello TORVO, I need account/order details older than 30 days. Dealer Code: ${user.dealer_code||'PLEASE CHECK MY ACCOUNT'}`;window.open(`https://wa.me/917027751533?text=${encodeURIComponent(text)}`,'_blank','noopener,noreferrer')};if(!user.dealer_id)return <section className="panel workspace"><div className="empty"><h3>Dealer account link required</h3><p>TORVO Admin must link this login to an approved dealer before ordering.</p></div></section>;return <div className="dealerPortal">{err&&<div className="empty"><h3>Unable to load dealer portal</h3><p>{err}</p><button onClick={load}>Retry</button></div>}<section className="welcome"><div><span className="eyebrow">APPROVED B2B DEALER</span><h2>Welcome, {user.full_name||'Dealer'}</h2><p>Select products, see your applicable amount, submit Purchase Orders and follow Estimate & Delivery.</p></div><div className="health"><i/>Secure dealer account</div></section><section className="panel productFinder"><header><div><h3>Product Finder</h3><p>Machine → Spare Part → Accessory. Search by TORVO code, OEM code, product, brand, category or model.</p></div><button className="iconBtn" disabled={busy} onClick={load}><RefreshCw size={18}/></button></header><SmartSearch value={search} onChange={setSearch} items={catalog} onCamera={()=>alert('Camera search pending service connection.')} onMic={()=>alert('Voice search pending service connection.')}/><div className="catalogFilters"><span><SlidersHorizontal size={15}/> FILTERS</span><select value={type} onChange={e=>{setType(e.target.value);setBrand('');setCategory('');setModel('')}}><option value="">All Products</option><option value="machine">Machine</option><option value="spare_part">Spare Part</option><option value="accessory">Accessory</option></select><select value={brand} onChange={e=>{setBrand(e.target.value);setCategory('');setModel('')}}><option value="">All Brands</option>{brands.map(x=><option key={x}>{x}</option>)}</select><select value={category} onChange={e=>{setCategory(e.target.value);setModel('')}}><option value="">All Categories</option>{categories.map(x=><option key={x}>{x}</option>)}</select><select value={model} onChange={e=>setModel(e.target.value)}><option value="">All Models</option>{models.map(x=><option key={x}>{x}</option>)}</select>{(type||brand||category||model||search)&&<button onClick={clearFilters}><X size={14}/>Clear All</button>}<b>{items.length} results</b></div><div className="dealerCatalog">{items.length===0?<div className="miniEmpty">No matching product. Clear filters or send a Missing Product Request.</div>:items.slice(0,80).map(x=><article key={x.id}><div>{x.image_url&&<img className="catalogThumb" src={x.image_url} alt=""/>}<b>{x.item_code}</b><strong>{x.name}</strong><small>{[x.oem_code&&`OEM ${x.oem_code}`,x.brand,x.category,x.model,x.item_type?.replace('_',' ')].filter(Boolean).join(' · ')}</small></div><div className="catalogItemActions">{x.item_type==='machine'&&<button className="machineSpareBtn" onClick={()=>openSpares(x)}><Wrench size={15}/>Spare Parts<ChevronRight size={14}/></button>}<input type="number" min="0" step="1" value={cart[x.id]||''} onChange={e=>qty(x.id,e.target.value)} placeholder="Qty"/></div></article>)}</div><button className="primary dealerSubmit" disabled={!Object.values(cart).some(x=>Number(x)>0)} onClick={submitPreview}><ShoppingCart size={16}/> Submit Purchase Order</button></section>{machine&&<section className="panel machineSparePanel"><header><div><span className="eyebrow">MACHINE SPARE PARTS</span><h3>{machine.name}</h3></div><button className="iconBtn" onClick={()=>setMachine(null)}><X size={18}/></button></header>{spareBusy?<div className="miniEmpty">Loading spare parts…</div>:<div className="dealerCatalog machineSpareList">{machineSpares.map(s=><article key={s.id}><div><b>{s.item_code}</b><strong>{s.name}</strong></div><input type="number" min="0" step="1" value={spareCart[s.id]||''} onChange={e=>spareQty(s.id,e.target.value)} placeholder="Qty"/></article>)}</div>}<button className="primary dealerSubmit" onClick={submitPreview}><ShoppingCart size={16}/> Add to Purchase Order</button></section>}<section className="panel"><header><div><h3>My Order History</h3><p>Only the latest 30 days are shown here. Payment/accounting information is never displayed in Dealer Portal.</p></div></header>{docs.length===0?<div className="miniEmpty">No Purchase Order / Estimate history in the last 30 days.</div>:<div className="requestList">{docs.map(d=>{const track=dispatch.find(x=>x.estimate_id===d.id);return <article key={d.id}><div><strong>{d.doc_type==='sales_order'?'PURCHASE ORDER':'ESTIMATE'}</strong><span>{d.status.toUpperCase()} · {money(d.final_payable||d.subtotal)}</span><small>{new Date(d.created_at).toLocaleDateString('en-GB')}{track?.tracking_code?` · Tracking: ${track.tracking_code}`:''}</small></div><b className={`status ${d.status}`}>{d.status}</b>{track&&<span><Truck size={14}/> {track.status.replaceAll('_',' ')}</span>}</article>})}</div>}<div className="miniEmpty"><strong>Need details older than 30 days?</strong><br/>Contact TORVO on WhatsApp. We will provide the required account/order details directly.<br/><button className="primary" onClick={olderDetails}><MessageSquare size={15}/> Contact WhatsApp for Account Details</button></div></section><section className="dealerTwo"><section className="panel"><header><div><h3>Schemes & Rewards</h3><p>Your available TORVO points and active schemes.</p></div><Gift size={18}/></header><div className="miniEmpty"><WalletCards size={15}/> Available: <strong>{rewardTotal.toLocaleString('en-IN')} points</strong></div>{schemes.length===0?<div className="miniEmpty">No active schemes currently.</div>:<div className="requestList compactList">{schemes.map(s=><article key={s.id}><div><strong>{s.name}</strong><span>{new Date(s.start_date).toLocaleDateString('en-GB')} → {new Date(s.end_date).toLocaleDateString('en-GB')}</span></div><b className="status approved">ACTIVE</b></article>)}</div>}</section><section className="panel"><header><div><h3>Missing Product Requests</h3><p>Ask TORVO for a product that is not in the catalog.</p></div><button className="iconBtn" disabled={busy} onClick={addMissing}><SearchX size={17}/></button></header>{missing.length===0?<div className="miniEmpty">No missing-product requests.</div>:<div className="requestList compactList">{missing.slice(0,20).map(r=><article key={r.id}><div><strong>{r.search_text}</strong><span>{r.item_type||'Unclassified'} · Qty {r.qty||'—'}</span></div><b className={`status ${r.status==='closed'||r.status==='sourced'?'approved':'pending'}`}>{r.status}</b></article>)}</div>}</section></section><section className="panel"><header><div><h3>Messages</h3><p>Secure communication with TORVO.</p></div><button className="iconBtn" disabled={busy} onClick={sendMsg}><MessageSquare size={17}/></button></header><div className="requestList compactList">{messages.slice(0,20).map(m=><article key={m.id}><div><strong>{m.subject||'Message'}</strong><span>{m.body}</span><small>{new Date(m.created_at).toLocaleString('en-GB')}</small></div></article>)}</div></section></div>
