import React,{useCallback,useEffect,useRef,useState}from'react';import{Download,RefreshCw,Smartphone,ShieldCheck}from'lucide-react';import{loadAppReleases,verifiedDownload,durableDownload,latestVerifiedRelease,latestProductionAndroidRelease,isProductionAndroidRelease,TORVO_ANDROID_PACKAGE}from'../services/appRelease';import'../app-release-ui.css';
const text=(v,fallback='—')=>{const s=String(v??'').trim();return s||fallback};
const statusOf=v=>text(v,'UNKNOWN').toUpperCase();
const platformOf=v=>text(v,'UNKNOWN').replaceAll('_',' ').toUpperCase();
const commitOf=v=>{const s=String(v??'').trim();return s?s.slice(0,12).toUpperCase():'NO SHA'};
const channelOf=v=>text(v,'UNSET').toUpperCase();
export default function AppReleaseCenter(){const[rows,setRows]=useState([]),[err,setErr]=useState(''),[busy,setBusy]=useState(false),mounted=useRef(true),busyRef=useRef(false);const load=useCallback(async()=>{if(busyRef.current)return;busyRef.current=true;if(mounted.current){setBusy(true);setErr('')}try{const next=await loadAppReleases();if(mounted.current)setRows(next)}catch(e){if(mounted.current)setErr(e instanceof Error?e.message:'RELEASE CENTER COULD NOT BE LOADED.')}finally{busyRef.current=false;if(mounted.current)setBusy(false)}},[]);useEffect(()=>{mounted.current=true;load();return()=>{mounted.current=false}},[load]);const latestAndroid=latestVerifiedRelease(rows,'android_apk'),productionAndroid=latestProductionAndroidRelease(rows);return <section className="panel workspace releaseCenter"><header><div><h3>APP RELEASE CENTER</h3><p>ONLY VERIFIED BUILD ARTIFACTS CAN BE DOWNLOADED. TEST APK FILES NEVER BECOME AUTOMATIC PRODUCTION UPDATES. NATIVE UPDATES MUST KEEP PACKAGE {TORVO_ANDROID_PACKAGE} AND A VERIFIED PRODUCTION SIGNATURE.</p>{productionAndroid?<small>LATEST PRODUCTION ANDROID UPDATE: VERSION {text(productionAndroid.version)} · BUILD {text(productionAndroid.build_number)} · COMMIT {commitOf(productionAndroid.commit_sha)}</small>:latestAndroid?<small>LATEST VERIFIED ANDROID FILE IS TEST/UNSIGNED OR NOT YET PUBLISHED FOR PRODUCTION UPDATE.</small>:null}</div><button type="button" className="iconBtn" onClick={load} disabled={busy} aria-label="REFRESH APP RELEASES" title="REFRESH APP RELEASES"><RefreshCw size={18}/></button></header>{err&&<div className="inlineError" role="alert">{text(err,'RELEASE CENTER COULD NOT BE LOADED.')}</div>}<div className="releaseGrid">{busy&&rows.length===0?<div className="miniEmpty">LOADING VERIFIED APP RELEASES…</div>:rows.length===0?<div className="miniEmpty">NO VERIFIED APP RELEASE HAS BEEN PUBLISHED YET.</div>:rows.map((r,index)=>{const verifiedUrl=verifiedDownload(r),production=isProductionAndroidRelease(r),productionUrl=production?durableDownload(r):null,url=production?productionUrl:verifiedUrl;const key=text(r?.id,`${platformOf(r?.platform)}-${text(r?.version,'0')}-${text(r?.build_number,index)}`);const isLatest=Boolean(productionAndroid&&r===productionAndroid);const blockedProduction=production&&!productionUrl;return <article key={key}><div><Smartphone/><strong>{platformOf(r?.platform)}{isLatest?' · LATEST PRODUCTION UPDATE':production?' · PRODUCTION':' · TEST/RELEASE FILE'}</strong><span>VERSION {text(r?.version)} · BUILD {text(r?.build_number)} · CHANNEL {channelOf(r?.channel)}</span><small>COMMIT {commitOf(r?.commit_sha)} · {statusOf(r?.status)}{r?.artifact_sha256?` · SHA256 ${text(r.artifact_sha256).slice(0,12).toUpperCase()}…`:''}</small></div>{url?<a className="primary" href={url} target="_blank" rel="noopener noreferrer"><Download size={15}/>{isLatest?'DOWNLOAD LATEST UPDATE':production?'DOWNLOAD PRODUCTION FILE':'DOWNLOAD VERIFIED TEST FILE'}</a>:<span className="releaseLocked"><ShieldCheck size={15}/>{blockedProduction?'PRODUCTION URL NOT DURABLE':'NOT VERIFIED FOR DOWNLOAD'}</span>}</article>})}</div></section>}
