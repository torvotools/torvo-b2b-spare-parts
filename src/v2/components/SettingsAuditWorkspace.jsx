import React,{useState}from'react';import{ShieldCheck,Settings,History,LockKeyhole}from'lucide-react';
export default function SettingsAuditWorkspace({role}){const[tab,setTab]=useState('settings');return <section className="panel workspace"><header><div><span className="eyebrow">SYSTEM CONTROL</span><h3>SETTINGS & AUDIT</h3><p>SECURITY-SENSITIVE SETTINGS STAY SERVER-CONTROLLED. NO FAKE AUDIT RECORDS ARE SHOWN.</p></div><ShieldCheck size={22}/></header><div className="workspaceTabs"><button className={tab==='settings'?'active':''} onClick={()=>setTab('settings')}><Settings size={15}/> SYSTEM SETTINGS</button><button className={tab==='audit'?'active':''} onClick={()=>setTab('audit')}><History size={15}/> AUDIT LOG</button></div>{tab==='settings'?<div className="adminActionGrid"><article className="panel"><LockKeyhole size={19}/><b>SECURE BUSINESS RULES</b><small>DELIVERY, APPROVAL, STOCK AND ROLE RULES ARE ENFORCED BY AUTHORIZED BACKEND FLOWS.</small></article><article className="panel"><ShieldCheck size={19}/><b>ROLE PROTECTION</b><small>{role==='owner'?'OWNER ACCESS · CONFIDENTIAL CONTROLS ALLOWED':'AUTHORIZED ACCESS · OWNER-ONLY CONFIDENTIAL CONTROLS HIDDEN'}</small></article></div>:<div className="empty"><History size={28}/><h3>AUDIT LOG</h3><p>LIVE AUDIT EVENTS WILL APPEAR ONLY FROM THE SECURE AUDIT SOURCE. THIS SCREEN DOES NOT INVENT HISTORY.</p></div>}</section>}
