import React,{useState}from'react';import{QrCode,Search,ShoppingCart,CheckCircle2,PackageCheck}from'lucide-react';import{confirmDealerReferralBenefit,dealerReferralSupply,orderDealerReferralItem,verifyDealerReferral}from'../services/dealerReferral';
const U=v=>String(v||'').toUpperCase();
export default function DealerReferralWorkspace(){const[code,setCode]=useState(''),[ref,setRef]=useState(null),[supply,setSupply]=useState(null),[qty,setQty]=useState(1),[busy,setBusy]=useState(false),[err,setErr]=useState(''),[ok,setOk]=useState('');const verify=async()=>{if(!code.trim())return;setBusy(true);setErr('');setOk('');setRef(null);setSupply(null);try{const row=await verifyDealerReferral(code);setRef(row||null);setSupply(await dealerReferralSupply(code))}catch(e){setErr(U(e.message))}finally{setBusy(false)}};const benefit=async()=>{setBusy(true);setErr('');try{await confirmDealerReferralBenefit(code);setOk('CUSTOMER BENEFIT CONFIRMED');setRef(v=>({...v,status:'benefit_given'}))}catch(e){setErr(U(e.message))}finally{setBusy(false)}};const order=async()=>{setBusy(true);setErr('');try{const id=await orderDealerReferralItem(code,qty);setOk(`TORVO PURCHASE ORDER CREATED: ${String(id).slice(0,8).toUpperCase()}`)}catch(e){setErr(U(e.message))}finally{setBusy(false)}};return <section className="panel workspace"><header><div><span className="eyebrow">CUSTOMER REFERRAL</span><h3>SCAN / ENTER TORVO CODE</h3><p>VERIFY THE CUSTOMER REFERRAL, GIVE THE APPLICABLE BENEFIT, AND ORDER THE SAME ITEM FROM TORVO IF REQUIRED.</p></div><QrCode size={22}/></header>{err&&<div className="inlineError">{err}</div>}{ok&&<div className="inlineSuccess">{ok}</div>}<div className="workFilters"><label><Search size={16}/><input value={code} onChange={e=>setCode(U(e.target.value))} placeholder="TV-XXXXX" autoCapitalize="characters"/></label><button className="primary" disabled={busy||!code.trim()} onClick={verify}>{busy?'CHECKING…':'VERIFY CODE'}</button></div>{ref&&<div className="requestList"><article><div><strong>CUSTOMER NEEDS: {U(ref.product_name)}</strong><span>REFERRAL STATUS: {U(ref.status).replaceAll('_',' ')}</span><small>BENEFIT: {ref.benefit_type?`${U(ref.benefit_type).replaceAll('_',' ')} ${ref.benefit_value??''}`:U(ref.benefit_text)||'TRACKING / CAMPAIGN RULE'}</small></div>{ref.status!=='benefit_given'&&<button disabled={busy} onClick={benefit}><CheckCircle2 size={14}/>BENEFIT GIVEN</button>}</article>{supply&&<article><div><strong>TORVO SUPPLY: {U(supply.torvo_supply_status)}</strong><span>{supply.torvo_supply_status==='AVAILABLE'?'AUTHORITATIVE TORVO INVENTORY CONFIRMS SUPPLY.':'NO LIVE TORVO SUPPLY IS BEING CLAIMED.'}</span></div>{supply.torvo_supply_status==='AVAILABLE'&&<div className="rowActions"><input type="number" min="1" step="1" value={qty} onChange={e=>setQty(Math.max(1,Math.floor(Number(e.target.value)||1)))}/><button className="primary" disabled={busy} onClick={order}><ShoppingCart size={14}/>ORDER FROM TORVO</button></div>}</article>}</div>}<div className="modalNotice"><PackageCheck size={16}/> CUSTOMER RETAIL PRICE IS NOT STORED HERE. TORVO DEALER PROCUREMENT RATE IS RESOLVED PRIVATELY BY THE BACKEND.</div></section>}
