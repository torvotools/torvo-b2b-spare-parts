import React,{useEffect,useState}from'react';import{RefreshCw,Phone,MessageCircle,XCircle}from'lucide-react';import{loadAdminCustomerLeads,closeAdminCustomerLead}from'../services/adminCustomerLeads';
const filters=['','submitted','sourcing','available','sent','accepted','declined','closed'];
export default function AdminCustomerLeads(){const[rows,setRows]=useState([]),[status,setStatus]=useState(''),[busy,setBusy]=useState(false),[err,setErr]=useState('');const load=async()=>{setBusy(true);setErr('');try{setRows(await loadAdminCustomerLeads(status))}catch(e){setErr(e?.message||'CUSTOMER LEADS UNAVAILABLE')}finally{setBusy(false)}};useEffect(()=>{load()},[status]);const close=async r=>{const reason=window.prompt('CLOSE REASON');if(!reason)return;setBusy(true);try{await closeAdminCustomerLead(r.demand_id,reason);await load()}catch(e){setErr(e?.message||'CLOSE FAILED');setBusy(false)}};return <section className="panel workspace"><header className="workspaceTitle"><div><span className="eyebrow">PRIVATE ADMIN WORKSPACE</span><h3>CUSTOMER LEADS CENTER</h3><p>CONFIRMED CUSTOMER REQUIREMENTS, DEALER RESPONSE AND TORVO SOURCING STATUS.</p></div><button onClick={load} disabled={busy}><RefreshCw size={15}/>REFRESH</button></header><div className="quickActions">{filters.map(x=><button key={x||'all'} className={status===x?'active':''} onClick={()=>setStatus(x)}>{x?x.toUpperCase():'ALL'}</button>)}</div>{err&&<div className="errorBox">{err}</div>}{!busy&&!rows.length&&!err&&<div className="empty"><h3>NO CUSTOMER LEADS</h3><p>CONFIRMED PRODUCT REQUIREMENTS WILL APPEAR HERE.</p></div>}{rows.map((r,i)=><article className="panel" key={`${r.demand_id}-${r.lead_id||i}`}><div className="sectionHead"><div><h3>{r.search_text}</h3><p>{r.pin_code} · {String(r.demand_status||'').toUpperCase()}{r.routing_stage?` · ${r.routing_stage}`:''}{r.lead_status?` · ${String(r.lead_status).toUpperCase()}`:''}</p></div></div><p><b>CUSTOMER:</b> {r.customer_name} · {r.mobile}</p>{r.brand&&<p><b>BRAND:</b> {r.brand}{r.model_number?` · MODEL: ${r.model_number}`:''}</p>}{r.dealer_name&&<p><b>ASSIGNED DEALER:</b> {r.dealer_name}</p>}{r.requirement_note&&<p>{r.requirement_note}</p>}<div className="quickActions"><a href={`tel:${r.mobile}`}><Phone size={15}/>CALL</a><a href={`https://wa.me/91${String(r.mobile||'').replace(/\D/g,'').slice(-10)}`} target="_blank" rel="noreferrer"><MessageCircle size={15}/>WHATSAPP</a>{!['closed','cancelled'].includes(String(r.demand_status||'').toLowerCase())&&<button onClick={()=>close(r)} disabled={busy}><XCircle size={15}/>CLOSE REQUIREMENT</button>}</div></article>)}</section>}
