import React,{useEffect,useState}from'react';import{Award,Gift,RefreshCw,Target,Trophy}from'lucide-react';import{claimReward,loadRewardBalance,loadRewardOptions}from'../services/dealerRewards';
export default function DealerTargetRewards(){const[balance,setBalance]=useState(0),[options,setOptions]=useState([]),[busy,setBusy]=useState(false),[msg,setMsg]=useState('');const load=async()=>{setBusy(true);setMsg('');try{const[b,o]=await Promise.all([loadRewardBalance(),loadRewardOptions()]);setBalance(b);setOptions(o)}catch(e){setMsg(e.message)}finally{setBusy(false)}};useEffect(()=>{load()},[]);const claim=async r=>{if(!r.available||busy)return;setBusy(true);setMsg('');try{await claimReward(r.id);setMsg('REWARD CLAIM SUBMITTED TO TORVO.');await load()}catch(e){setMsg(e.message)}finally{setBusy(false)}};return <section className="dealerRewards"><div className="rewardHero"><div><span className="eyebrow">APRIL → MARCH ANNUAL SCHEME</span><h3>TORVO TARGET REWARDS</h3><p>YOUR ELIGIBLE PURCHASE VALUE REACHES TORVO TARGET SLABS TO EARN POINTS. POINTS CAN BE USED FOR AVAILABLE GIFTS OR VOUCHERS.</p></div><div className="rewardBalance"><Trophy/><b>{balance}</b><span>AVAILABLE POINTS</span></div></div>{msg&&<div className="adminSearchState">{msg}</div>}<div className="securityNote"><Target size={16}/> POINTS ARE EARNED ONLY FROM ADMIN-DEFINED TARGET SLABS — NOT FROM FITMENT SUGGESTIONS OR PER-INVOICE RANDOM POINTS.</div><section className="panel"><header><div><h3>REWARD OPTIONS</h3><p>CLAIM BECOMES AVAILABLE AUTOMATICALLY WHEN YOUR POINT BALANCE IS ENOUGH.</p></div><button className="iconBtn" disabled={busy} onClick={load}><RefreshCw size={17}/></button></header><div className="rewardGrid">{options.map(r=><article key={r.id} className={`rewardCard ${r.available?'available':''}`}><div className="rewardIcon">{r.redemption_type==='voucher'?<Award/>:<Gift/>}</div><strong>{r.name}</strong><span>{r.redemption_type.toUpperCase()} · {r.points_required} POINTS</span>{Number(r.gift_value)>0&&<small>REFERENCE VALUE ₹{Number(r.gift_value).toLocaleString('en-IN')}</small>}<button className={r.available?'primary':'secondary'} disabled={!r.available||busy} onClick={()=>claim(r)}>{r.available?'CLAIM REWARD':`NEED ${Math.max(0,r.points_required-balance)} MORE POINTS`}</button></article>)}</div>{!options.length&&<div className="miniEmpty">NO ACTIVE REWARD OPTIONS RIGHT NOW.</div>}</section></section>}
