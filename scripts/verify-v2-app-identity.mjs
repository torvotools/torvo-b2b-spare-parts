import{readFile}from'node:fs/promises';
const[capText,releaseSql,releaseService,apk]=await Promise.all([readFile('capacitor.config.json','utf8'),readFile('supabase/v2-app-release-center.sql','utf8'),readFile('src/v2/services/appRelease.js','utf8'),readFile('.github/workflows/v2-android-apk.yml','utf8')]);const config=JSON.parse(capText);const checks=[['PACKAGE ID',config.appId==='com.torvotools.app'],['APP NAME',config.appName==='TORVO TOOLS'],['APP WEB DIRECTORY',config.webDir==='dist-app'],['HTTPS ANDROID SCHEME',config.server?.androidScheme==='https'],['NO MIXED CONTENT',config.android?.allowMixedContent===false],['BACKEND PACKAGE CONTINUITY',releaseSql.includes("package_id='com.torvotools.app'")],['CLIENT PACKAGE CONTINUITY',releaseService.includes("TORVO_ANDROID_PACKAGE='com.torvotools.app'")],['APK PACKAGE CONTINUITY',apk.includes('applicationId "com.torvotools.app"')&&apk.includes('namespace "com.torvotools.app"')],['TEST CHANNEL NON PRODUCTION',apk.includes('"channel":"TEST-DEBUG"')&&apk.includes('"production_signed":false')]];let failed=false;for(const[n,ok]of checks){console.log(`${ok?'PASS':'FAIL'} ${n}`);if(!ok)failed=true}if(failed)process.exit(1);console.log(`TORVO TOOLS APP IDENTITY VERIFIED (${checks.length} GATES)`);
