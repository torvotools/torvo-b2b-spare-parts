import fs from'node:fs';const sql=fs.readFileSync('supabase/v2-dealer-final-approval-accountant-gate.sql','utf8'),order=fs.readFileSync('supabase/V2_INSTALL_ORDER.md','utf8');
const checks=[['OWNER ADMIN ONLY',sql.includes("a.role not in('owner','admin')")],['ACCOUNTANT GATE',sql.includes("accountant_verification_status,'pending_accountant')<>'submitted_to_admin'")],['REGISTERED EMAIL REQUIRED',sql.includes('VALID REGISTERED EMAIL REQUIRED')&&sql.includes("'login_identity','REGISTERED_EMAIL'")],['SYSTEM DEALER CODE',sql.includes('code:=next_dealer_code()')&&!sql.includes('p_dealer_code text')],['CANONICAL APP IDENTITY',sql.includes("role,active,dealer_id")&&sql.includes("'dealer',true,p_dealer")],['AMBIGUITY REJECTED',sql.includes('DEALER AUTH IDENTITY AMBIGUOUS')],['LEGACY 3 ARG CLIENT ACCESS RETIRED',sql.includes('revoke all on function approve_dealer(uuid,text,text) from public,anon,authenticated')],['INSTALL ORDERED',order.includes('v2-accountant-dealer-verification.sql')&&order.includes('v2-dealer-final-approval-accountant-gate.sql')]];
const failed=checks.filter(([,ok])=>!ok);for(const[n,ok]of checks)console.log(`${ok?'PASS':'FAIL'} ${n}`);if(failed.length)process.exit(1);console.log(`PASS FINAL DEALER APPROVAL (${checks.length} GATES)`);