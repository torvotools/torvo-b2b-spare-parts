import fs from'node:fs';const read=p=>fs.readFileSync(p,'utf8');const settlement=read('supabase/v2-dealer-target-reward-settlement.sql'),admin=read('supabase/v2-dealer-target-reward-admin-read.sql'),ui=read('src/v2/components/TargetRewardAdmin.jsx'),service=read('src/v2/services/dealerRewards.js');const must=(ok,msg)=>{if(!ok)throw new Error(msg)};must(settlement.includes("d.doc_type='estimate'")&&settlement.includes("x.status='delivered'")&&settlement.includes('x.delivered_at'),'TARGET SETTLEMENT MUST USE ACTUAL DELIVERED ESTIMATE');must(settlement.includes('d.final_payable'),'TARGET SETTLEMENT MUST USE DELIVERED ESTIMATE VALUE');must(settlement.includes('unique(dealer_id,scheme_year,target_slab_id)'),'TARGET SLAB SETTLEMENT MUST BE IDEMPOTENT');must(admin.includes('reward_admin_user()'),'ADMIN READ MODEL MUST ENFORCE OWNER/ADMIN');must(service.includes('admin_dealer_target_reward_data'),'ADMIN SERVICE NOT CONNECTED');must(ui.includes('APRIL → MARCH')&&ui.includes('NO MANUAL PER-INVOICE POINTS'),'FINAL APRIL-MARCH TARGET RULE MISSING');must(!ui.includes('Earn Points')&&!ui.includes('EARN POINTS'),'ARBITRARY MANUAL EARN CONTROL MUST NOT EXIST IN FINAL TARGET ADMIN');must(ui.includes('approveRewardClaim')&&ui.includes('cancelRewardClaim')&&ui.includes('issueRewardVoucher'),'CLAIM ADMIN ACTIONS MISSING');console.log('TORVO V2 FINAL TARGET/REWARD CONTRACT OK');
