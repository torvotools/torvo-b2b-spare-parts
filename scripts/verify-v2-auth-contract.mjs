import fs from'node:fs';
const read=p=>fs.readFileSync(p,'utf8');
const pin=read('supabase/v2-dealer-pin-auth.sql'),missing=read('supabase/v2-dealer-missing-part-request.sql'),catalogSql=read('supabase/v2-dealer-catalog-search.sql'),machineSql=read('supabase/v2-dealer-machine-spares.sql'),referralNetwork=read('supabase/v2-customer-dealer-referral-network.sql'),referralOrder=read('supabase/v2-referral-to-b2b-order-conversion.sql'),staffAccess=read('supabase/v2-admin-issued-staff-access.sql'),staffSession=read('supabase/v2-staff-whatsapp-auth.sql'),worker=read('supabase/functions/STAFF_ONE_TIME_LOGIN_CONTRACT.md'),install=read('supabase/V2_INSTALL_ORDER.md'),staff=read('src/v2/services/staffAuth.js'),session=read('src/v2/services/dealerSession.js'),auth=read('src/v2/services/auth.js'),repo=read('src/v2/services/repository.js'),referral=read('src/v2/services/dealerReferral.js'),adminStaff=read('src/v2/services/adminStaffAccess.js');
const guarded=name=>repo.includes(`async ${name}`)&&repo.slice(repo.indexOf(`async ${name}`),repo.indexOf(`async ${name}`)+180).includes('await dealerGuard()');
const checks=[['ONE ACTIVE DEALER DEVICE',pin.includes('uq_dealer_one_active_device')&&pin.includes("revoke_reason='NEW_DEVICE_LOGIN'")],['DEALER DEVICE ASSERTION',pin.includes('dealer_assert_my_device_session')&&pin.includes('auth.uid()')],['DEALER HEARTBEAT',session.includes('startDealerSessionWatch')],['PURCHASE ORDER GUARDED',guarded('submitPurchaseOrder')],['REFERRAL DEVICE PROOF',referral.includes('p_device_id:proof.deviceId')&&referralNetwork.includes('dealer_assert_my_device_session')&&referralOrder.includes('dealer_assert_my_device_session')],['MISSING/CATALOG/FITMENT DEVICE BOUND',missing.includes('dealer_assert_my_device_session')&&catalogSql.includes('dealer_assert_my_device_session')&&machineSql.includes('dealer_assert_my_device_session')],['STAFF USERNAME ACCESS',staffAccess.includes('staff_access_identities')&&staffAccess.includes('uq_staff_access_username_upper')],['STAFF PASSWORD HASH AND CONSUME',staffAccess.includes("crypt(p_password,gen_salt('bf'))")&&staffAccess.includes('used_at=now()')],['ONE APPROVED STAFF DEVICE',staffAccess.includes('uq_staff_one_active_device')],['ACCOUNTANT DESKTOP ONLY',staffAccess.includes("i.staff_role='accountant' and p_device_type<>'desktop'")],['FIELD STAFF MOBILE ONLY',staffAccess.includes("i.staff_role in('salesman','store_keeper') and p_device_type<>'mobile_app'")],['REPLACEMENT DEVICE REVOKES OLD',staffAccess.includes('update staff_authorized_devices set revoked_at=now() where app_user_id=p_app_user_id and revoked_at is null')],['ONE TIME VERIFY TRUSTED ONLY',staffAccess.includes('revoke all on function staff_verify_one_time_password(text,text,text,text) from public,anon,authenticated')],['CLIENT USES ONE TIME LOGIN WORKER',staff.includes("functions.invoke('staff-one-time-login'")&&staff.includes('loginStaffWithAdminPassword')],['VERIFIED SESSION SUPPORTS ONE TIME METHOD',staffSession.includes("'admin_one_time_password'")&&staffSession.includes("v.role not in('accountant','salesman','store_keeper')")],['EMPLOYEE EMERGENCY BYPASS CLOSED',staffSession.includes("role in('owner','admin')")&&staffSession.includes('OWNER_OR_ADMIN_ACCESS_DENIED')],['WORKER CONTRACT CONSUMES BEFORE SESSION',worker.includes('staff_verify_one_time_password')&&worker.includes('staff_create_verified_session')],['WORKER CONTRACT HIDES SECRETS',worker.includes('Never return password/hash/service secrets')],['ADMIN STAFF CONTROLS',adminStaff.includes('issueStaffOneTimePassword')&&adminStaff.includes('approveStaffDevice')&&adminStaff.includes('revokeStaffAccess')],['INSTALL ORDER NEW STAFF MODEL',install.includes('v2-admin-issued-staff-access.sql')&&install.includes('ACCOUNTANT IS DESKTOP-ONLY')],['DEALER SIGNOUT REVOKES SERVER',auth.includes("functions.invoke('dealer-session-revoke'")]];
const failed=checks.filter(([,ok])=>!ok);for(const[name,ok]of checks)console.log(`${ok?'PASS':'FAIL'} ${name}`);if(failed.length){console.error(`AUTH CONTRACT FAILED: ${failed.length} CHECK(S)`);process.exit(1)}console.log('TORVO V2 AUTH CONTRACT VERIFIED');
