import fs from'node:fs';
const read=p=>fs.readFileSync(p,'utf8');
const pin=read('supabase/v2-dealer-pin-auth.sql'),missing=read('supabase/v2-dealer-missing-part-request.sql'),staff=read('src/v2/services/staffAuth.js'),session=read('src/v2/services/dealerSession.js'),dealerMissing=read('src/v2/services/dealerMissingPart.js'),auth=read('src/v2/services/auth.js'),app=read('src/v2/App.jsx'),repo=read('src/v2/services/repository.js'),referral=read('src/v2/services/dealerReferral.js'),referralUi=read('src/v2/components/DealerReferralWorkspace.jsx');
const guarded=name=>repo.includes(`async ${name}`)&&repo.slice(repo.indexOf(`async ${name}`),repo.indexOf(`async ${name}`)+180).includes('await dealerGuard()');
const checks=[['ONE ACTIVE DEALER DEVICE INDEX',pin.includes('uq_dealer_one_active_device')&&pin.includes('where revoked_at is null')],['NEW DEVICE REVOKES OLD SESSION',pin.includes("revoke_reason='NEW_DEVICE_LOGIN'")],['PIN CHANGE REVOKES SESSION',pin.includes("revoke_reason='PIN_CHANGED'")],['TRUSTED SESSION FUNCTIONS PRIVATE',pin.includes('revoke all on function dealer_start_device_session')&&pin.includes('revoke all on function dealer_validate_device_session')],['AUTHENTICATED DEVICE ASSERTION EXISTS',pin.includes('dealer_assert_my_device_session')&&pin.includes('auth.uid()')&&pin.includes('dealer_validate_device_session(v_dealer_id')],['DEVICE ASSERTION DOES NOT ACCEPT DEALER ID',pin.includes('dealer_assert_my_device_session(p_device_id text,p_session_token text)')],['DEALER CLIENT REQUIRES OPAQUE TOKEN',staff.includes("if(!data?.ok||!data?.dealer_session_token)")&&staff.includes('DEALER_SESSION_KEY')],['FAILED LOGIN CLEARS OLD DEALER TOKEN',staff.includes('clearDealerSession();const{data,error}=await client.functions.invoke')],['DEVICE ID MUST BE SECURE',staff.includes('crypto?.randomUUID')&&staff.includes('assertDevice(id)')],['APP VALIDATES DEALER DEVICE SESSION',session.includes("functions.invoke('dealer-session-valid'")&&app.includes('enforceDealerSession')],['SESSION VALIDATION FAILS CLOSED',session.includes('return invalidate()')&&session.includes('validationPromise')],['INVALIDATION PRESERVES TOKEN FOR SERVER REVOKE',session.includes('try{await forceSignOut()}catch{clearDealerSession()}')&&!session.includes('clearDealerSession();if(message)')],['DEALER SESSION HEARTBEAT EXISTS',session.includes('startDealerSessionWatch')&&session.includes('setInterval')&&session.includes("visibilitychange")&&session.includes("focus")],['APP STARTS DEALER SESSION WATCH',app.includes('startDealerSessionWatch(user)')],['REVOKED DEVICE HAS DEDICATED UX',session.includes('DEALER_SESSION_REVOKED_EVENT')&&app.includes('DEALER SESSION ENDED')],['PRIVATE DEALER ACTION ASSERTION EXISTS',session.includes('export async function assertDealerSession')&&session.includes('ACCOUNT IS ACTIVE ON ANOTHER DEVICE')],['DEALER RATE GUARDED',guarded('dealerItemRate')],['PURCHASE ORDER GUARDED',guarded('submitPurchaseOrder')],['DEALER REVISION GUARDED',guarded('dealerReviseSalesOrder')],['DEALER CHANGE REQUEST GUARDED',guarded('dealerRequestSalesOrderChange')],['DEALER ADD ON GUARDED',guarded('dealerCreateAddOnOrder')],['DEALER CONFIRMATION GUARDED',guarded('dealerConfirmSalesOrder')],['REFERRAL SERVICE VALIDATES ACTIVE DEVICE',referral.includes('await assertDealerSession()')],['MISSING PART SERVER BINDS DEVICE',missing.includes('dealer_assert_my_device_session(p_device_id,p_session_token)')],['LEGACY MISSING PART RPCS DROPPED',missing.includes('drop function if exists public.dealer_my_profile();')&&missing.includes('drop function if exists public.dealer_my_missing_part_requests();')],['MISSING PART CLIENT PASSES DEVICE PROOF',dealerMissing.includes('p_device_id:proof.deviceId')&&dealerMissing.includes('p_session_token:proof.token')],['PROFILE CLIENT PASSES DEVICE PROOF',auth.includes("client.rpc('dealer_my_profile',{p_device_id:device,p_session_token:token})")],['SIGN OUT REVOKES SERVER DEALER SESSION',auth.includes("functions.invoke('dealer-session-revoke'")&&auth.includes('dealer_session_token')],['CLIENT REJECTS DUPLICATE ACTIVE APP USER',auth.includes('limit(2)')&&auth.includes('ACCOUNT IDENTITY AMBIGUOUS')],['SERVER ASSERTION REJECTS AMBIGUOUS USER',pin.includes('DEALER AUTH IDENTITY AMBIGUOUS')],['SERVER ASSERTION REJECTS AMBIGUOUS DEALER',pin.includes('DEALER LINK AMBIGUOUS')]];
const failed=checks.filter(([,ok])=>!ok);for(const[name,ok]of checks)console.log(`${ok?'PASS':'FAIL'} ${name}`);if(failed.length){console.error(`AUTH CONTRACT FAILED: ${failed.length} CHECK(S)`);process.exit(1)}console.log('TORVO V2 AUTH CONTRACT VERIFIED');
