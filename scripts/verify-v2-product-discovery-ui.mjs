import fs from'node:fs';const r=p=>fs.readFileSync(p,'utf8'),u=r('src/v2/components/ProductDiscoveryRail.jsx'),s=r('src/v2/services/productPromotion.js'),c=r('src/v2/product-discovery.css'),m=r('src/v2/main.jsx');const g=[['PHOTO PROMOTIONS',u.includes('loadProductPromotions')&&u.includes('image_url')],['POPULAR ITEMS',u.includes('loadPopularProducts')&&u.includes('POPULAR ITEMS')],['WEBSITE TARGET',u.includes("target='website'")],['DEALER TARGET SUPPORTED',s.includes("target==='dealer_app'")],['NO PRICE IN DISCOVERY',!u.includes('₹')&&!u.toLowerCase().includes('price')],['EMPTY SAFE STATE',u.includes('ADMIN ACTIVATES THEM')],['MOBILE HORIZONTAL UI',c.includes('@media(max-width:640px)')&&c.includes('overflow-x:auto')],['STYLE LOADED',m.includes("'./product-discovery.css'")]];let b=0;for(const[n,v]of g){console.log(`${v?'PASS':'FAIL'} ${n}`);if(!v)b++}if(b)process.exit(1);console.log(`TORVO PRODUCT DISCOVERY UI VERIFIED (${g.length} GATES)`);
