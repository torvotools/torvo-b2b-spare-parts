import fs from'node:fs';const s=fs.readFileSync('supabase/v2-customer-lead-notifications.sql','utf8');const c=[['ADMIN ONLY',/u\.role not in\('owner','admin'\)/.test(s)],['ASSIGNED SENT LEAD',/status='sent'/.test(s)],['APPROVED DEALER',/x\.status='approved'/.test(s)],['PRIVATE DIRECT RECIPIENT',/app_notification_inbox\(campaign_id,user_id\) values\(cid,dealer_user\)/.test(s)],['PUSH OUTBOX',/app_push_outbox/.test(s)],['NO CUSTOMER CONTACT',!/customer_name|mobile|whatsapp/.test(s)],['AREA REQUIREMENT ONLY',/NEW CUSTOMER REQUIREMENT/.test(s)&&/AREA /.test(s)&&/OPEN LEAD/.test(s)],['AUDITED',/CUSTOMER_LEAD_DEALER_NOTIFIED/.test(s)],['NO ANON',/revoke all on function admin_notify_customer_demand_dealer\(uuid\) from public,anon/.test(s)]];const f=c.filter(([,x])=>!x);for(const[n,x]of c)console.log(`${x?'PASS':'FAIL'} ${n}`);if(f.length)process.exit(1);console.log(`PASS CUSTOMER LEAD NOTIFICATIONS (${c.length} GATES)`);