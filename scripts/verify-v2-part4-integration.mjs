import fs from'node:fs';const r=p=>fs.readFileSync(p,'utf8');const app=r('src/v2/App.jsx'),dealer=r('src/v2/components/DealerPortal.jsx'),search=r('src/v2/components/SmartSearch.jsx'),reports=r('src/v2/components/ReportsWorkspace.jsx'),notifications=r('src/v2/components/Notifications.jsx');const gates=[['ROLE ROUTES',app.includes("role==='salesman'")&&app.includes("role==='store_keeper'")&&app.includes("role==='accountant'")],['REPORTS CENTER',app.includes('<ReportsWorkspace role={role}/>')&&reports.includes('REPORTS CENTER')&&reports.includes('EXCEL (.XLS)')&&reports.includes('PRINT / PDF')],['NOTIFICATION BELL',app.includes('<Notifications open={notOpen}')&&notifications.includes('MARK ALL READ')],['CAMERA BEFORE MIC',search.indexOf('CAMERA SEARCH')<search.indexOf('VOICE SEARCH')],['GROUP LABELS',search.includes("'MACHINE'")&&search.includes("'SPARE PART'")&&search.includes("'ACCESSORY'")],['NO SEARCH ALL CONTROL',!search.includes('ALL PRODUCTS')&&!search.includes('ALL BRANDS')&&!search.includes('ALL CATEGORIES')&&!search.includes('ALL MODELS')],['DEALER DEVICE BRIDGE',dealer.includes('secureDealerLegacy')],['DEALER OK',dealer.includes('dealerConfirmSalesOrder')],['ADD MORE ITEMS',dealer.includes('add_more_items')]];let bad=0;for(const[n,v]of gates){console.log(`${v?'PASS':'FAIL'} ${n}`);if(!v)bad++}if(bad)process.exit(1);console.log(`TORVO PART 4 INTEGRATION VERIFIED (${gates.length} GATES)`);