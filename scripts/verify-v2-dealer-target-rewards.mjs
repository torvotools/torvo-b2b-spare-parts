import fs from'node:fs';const r=p=>fs.readFileSync(p,'utf8'),s=r('supabase/v2-dealer-target-rewards.sql'),k=r('supabase/v2-knowledge-rewards.sql'),c=r('src/v2/services/dealerRewards.js');const x=[['TARGET TYPES',s.includes('dealer_target_types')&&s.includes('dealer_target_slabs')],['APRIL SCHEME YEAR',s.includes('scheme_start_month int not null default 4')],['VALUE TO POINTS',s.includes('target_value')&&s.includes('points int not null')],['NO KNOWLEDGE POINTS',k.includes('NEVER earn TORVO Points')],['ONE YEAR LEDGER',s.includes('scheme_year')&&s.includes('dealer_reward_points_ledger')],['GIFT OR VOUCHER',s.includes("'gift','voucher'")],['AMAZON FLIPKART',s.includes("'AMAZON','FLIPKART'")],['CLAIM DEBITS POINTS',s.includes("'claim_debit'")&&s.includes('-r.points_required')],['DEALER SERVICE',c.includes('dealer_reward_balance')&&c.includes('dealer_claim_reward')],['ADMIN VOUCHER AUDIT',s.includes('DEALER_REWARD_VOUCHER_ISSUED')]];let b=0;for(const[n,o]of x){console.log(`${o?'PASS':'FAIL'} ${n}`);if(!o)b++}if(b)process.exit(1);console.log(`TORVO V2 DEALER TARGET REWARDS VERIFIED (${x.length} GATES)`);