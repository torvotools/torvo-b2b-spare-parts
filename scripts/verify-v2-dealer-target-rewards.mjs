import fs from'node:fs';const r=p=>fs.readFileSync(p,'utf8'),s=r('supabase/v2-dealer-target-rewards.sql'),k=r('supabase/v2-knowledge-rewards.sql'),c=r('src/v2/services/dealerRewards.js'),u=r('src/v2/components/DealerTargetRewards.jsx'),o=r('supabase/V2_INSTALL_ORDER.md');const x=[['TARGET TYPES',s.includes('dealer_target_types')&&s.includes('dealer_target_slabs')],['APRIL-MARCH YEAR',s.includes('extract(month from current_date)>=4')],['VALUE TO POINTS',s.includes('target_value')&&s.includes('points int not null')],['NO KNOWLEDGE POINTS',k.includes('NEVER earn TORVO Points')&&u.includes('NOT FROM FITMENT SUGGESTIONS')],['ONE YEAR LEDGER',s.includes('scheme_year')&&s.includes('dealer_reward_points_ledger')],['GIFT OR VOUCHER',s.includes("'gift','voucher'")],['AMAZON FLIPKART',s.includes("'AMAZON','FLIPKART'")],['CLAIM DEBITS POINTS',s.includes("'claim_debit'")&&s.includes('-r.points_required')],['DEALER BALANCE SERVICE',c.includes("dealerRpc('dealer_reward_balance')")],['PROGRESS SERVICE',c.includes("dealerRpc('dealer_reward_progress')")&&u.includes('rewardProgress')],['CLAIM HISTORY',c.includes("dealerRpc('dealer_reward_claim_history')")&&u.includes('MY REWARD CLAIMS')],['OWNER ADMIN CONFIG',s.includes('reward_admin_user')&&s.includes("role in('owner','admin')")],['ADMIN VOUCHER AUDIT',s.includes('DEALER_REWARD_VOUCHER_ISSUED')],['RLS PRIVATE',s.includes('alter table dealer_reward_claims enable row level security')&&s.includes('dealer_reward_claims from anon,authenticated')],['INSTALL ORDER',o.includes('v2-dealer-target-rewards.sql')]];let b=0;for(const[n,v]of x){console.log(`${v?'PASS':'FAIL'} ${n}`);if(!v)b++}if(b)process.exit(1);console.log(`TORVO V2 DEALER TARGET REWARDS VERIFIED (${x.length} GATES)`);
