import{readFile}from'node:fs/promises';
const[login,portal,attendance,report,accountant,admin]=await Promise.all([readFile('src/v2/components/BusinessLogin.jsx','utf8'),readFile('src/v2/components/DealerPortal.jsx','utf8'),readFile('src/v2/components/SalesmanAttendance.jsx','utf8'),readFile('src/v2/components/SalesmanAttendanceReport.jsx','utf8'),readFile('src/v2/components/AccountantVisualPreview.jsx','utf8'),readFile('src/v2/components/AdminVisualPreview.jsx','utf8')]);const checks=[['LOGIN CONTINUE SUBMITS',login.includes('<form onSubmit={start}>')&&login.includes('loginContinue')],['LOGIN VERIFY SUBMITS',login.includes('<form onSubmit={finish}>')&&login.includes('VERIFY & LOGIN')],['REGISTER DEALER NAVIGATES',login.includes("location.href='/'")&&login.includes('REGISTER AS DEALER')],['DEALER PO ACTION WIRED',portal.includes('SUBMIT PURCHASE ORDER')&&portal.includes('submitPurchaseOrder')],['DEALER OK ACTION WIRED',portal.includes('GIVE DEALER OK')&&portal.includes('dealerApproveSalesOrder')],['ADD MORE ITEMS WIRED',portal.includes('ADD MORE ITEMS')&&portal.includes('submitAdditionalPurchaseOrder')],['SALESMAN CHECK IN WIRED',attendance.includes('attendanceCheckIn')&&attendance.includes('CHECK IN')],['SALESMAN CHECK OUT WIRED',attendance.includes('attendanceCheckOut')&&attendance.includes('CHECK OUT')],['ADMIN ATTENDANCE STATUS WIRED',report.includes('adminSetAttendanceStatus')&&report.includes('onChange')],['ACCOUNTANT REQUIREMENT WIRED',accountant.includes('submitAccountantStockRequirement')&&accountant.includes('SUBMIT REQUIREMENT')],['ADMIN SEARCH WIRED',admin.includes('AdminUniversalSearch')&&admin.includes('searchProducts')&&admin.includes('openProduct')]];let bad=false;for(const[n,ok]of checks){console.log(`${ok?'PASS':'FAIL'} ${n}`);if(!ok)bad=true}if(bad)process.exit(1);console.log(`TORVO V2 FUNCTIONAL BUTTON ACTIONS VERIFIED (${checks.length} GATES)`);
